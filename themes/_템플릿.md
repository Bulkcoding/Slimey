# 테마: <테마 이름> (`<슬러그>`)

> 이 파일은 이 테마 세션이 **작업하면서 직접 갱신**한다.
> 평소 자동으로 읽지 않고, **사용자가 요청할 때만** 읽는 문서다.

- **브랜치 / 폴더**: `theme/<슬러그>` / `../Slimey-<슬러그>`
- **enum**: `SlimeSkinKind.<파스칼> = <번호>` (예약표에서 배정받은 번호)
- **설정창 표시 이름**: `"<한글>"`
- **상태**: ⬜ 진행 중 / ✅ 완료 / 🔀 머지됨

---

## 1. 컨셉 / 디자인
- 이 테마가 무엇인지, 어떤 비주얼을 목표로 했는지.
- 단단한 공(Rigid)인지, 표정/클릭 이펙트가 있는지 등 동작 특성.

## 2. 만든 / 수정한 파일
**신규**
- `src/Slimey/Views/Skins/<파스칼>Skin.xaml`
- `src/Slimey/Views/Skins/<파스칼>Skin.xaml.cs`

**공유 파일 append (충돌 핫스팟)**
- `Models/SlimeSkinKind.cs` — enum `<파스칼> = <번호>` 추가
- `Views/SlimeWindow.xaml.cs` → `ApplySkin()` case 추가
- `Views/SettingsWindow.xaml.cs` → `Skins[]` + `MakeSkin()` 추가

## 3. 구현 메모
- 그리기 방식(그라데이션·패턴·클립 등), 참고한 기존 스킨, 까다로웠던 점.

## 4. 검증
- [ ] 빌드 0 경고 / 0 오류
- [ ] 설정창 테마 칩에 표시되고 선택 시 즉시 교체됨
- [ ] 드래그·던지기·충돌 정상 (Rigid/이펙트 의도대로)
- [ ] 유휴 시 CPU ≈ 0
- [ ] **숨기기 단축키 → 이 테마가 띄운 것까지 전부 사라짐** (규칙 §3.6)
- [ ] **다시 보이기 → 숨기기 직전 상태 그대로 복원**

## 5. 남은 일 / 알려진 이슈
- (있으면 기록)
