# ThrowMe — 멀티 PC 인터넷 확장 (B안: 릴레이 서버 + 로그인) 설계

> 사용자가 **로그인**한 여러 PC를, **같은 LAN이 아니어도**(집↔집, 집↔사무실) 하나로 묶어
> 슬라임(공)이 한 PC 화면 밖으로 나가면 인터넷을 건너 다음 PC로 이어져 날아가게 한다.
> 기존 [멀티 PC 공 전달(LAN)] 설계의 "연결 계층"만 **릴레이 서버 경유**로 교체한 확장판.

- **확정 범위**: 인터넷을 넘는 임의 위치의 PC들, **로그인 기반 그룹핑**, **중앙 릴레이 서버 경유**
- **핵심 원칙(유지)**: 공은 항상 **한 PC만 소유**(공 = 토큰). 소유 PC만 물리를 돌린다.
- **핵심 변경**: 소유권 중재와 메시지 전달을 **서버가 권위(authoritative)로 중계**한다.
- **비용 원칙**: **결제 0원 프로토타입**부터 시작(무료 티어 + Let's Encrypt), 규모 커지면 스펙업.

---

## 0. 이 문서의 위치 / 기존 문서와의 관계

| 문서 | 범위 | 상태 |
|------|------|------|
| `ThrowMe_멀티PC_공전달_설계.md` | **같은 LAN**, 수동 IP, PC↔PC 직접 TCP | 설계 완료(미구현) |
| **이 문서 (B안)** | **인터넷**, 로그인, 릴레이 서버 경유 | 설계 |

> LAN 설계의 **물리·좌표 정규화·토큰 개념·핸드오프 메시지 필드는 그대로 재사용**한다.
> 이 문서는 그중 **"어떻게 서로 연결되고 누가 중재하는가"(연결 계층)만** 인터넷용으로 바꾼다.

---

## 1. 목표와 범위

- **된다**: 로그인한 내 PC들(사무실 데스크톱 + 집 데스크톱 + 노트북 등)을 한 그룹으로 묶어 공을 주고받는다.
- **NAT/방화벽 무관**: 각 PC가 서버로 **아웃바운드 연결**만 하므로 공유기 뒤에 있어도 된다(포트포워딩·IP지정 불필요).
- **해상도/DPI 무관**: LAN 설계의 좌표 정규화(t∈[0,1] + 엣지 기준 속도)를 그대로 사용.
- **범위 밖(이번 아님)**: 화상통화급 실시간 동기화, 대규모 공개 멀티플레이, 모바일.

---

## 2. 왜 릴레이 서버인가 (NAT 요약)

| 방식 | 집↔집 가능? | 사용자 설정 | 이 앱 적합도 |
|------|:---:|------|------|
| 수동 IP + 직접 TCP (LAN 설계) | ❌ (NAT 벽) | 포트포워딩 | 사무실만 |
| 메시 VPN(Tailscale) 얹기 (A안) | ✅ | VPN 설치·로그인 | 프로토타입 |
| **릴레이 서버 + 로그인 (B안)** | ✅ | **앱 로그인뿐** | **제품화** |

- 집 PC는 공인 IP가 없고 수시로 바뀐다 → **직접 연결 불가**.
- 서버로의 **아웃바운드 WebSocket 연결은 방화벽이 대부분 허용** → NAT 문제를 원천 회피.
- 서버가 "지금 이 그룹의 어떤 PC가 온라인인지" 알고 **메시지를 중계**하고 **소유권을 중재**한다.

---

## 3. 아키텍처

```
                    ┌─────────────────────────────┐
                    │        Relay Server         │
                    │  (클라우드, 상시 가동)       │
                    │  · 로그인/인증              │
                    │  · Room(그룹) & 엣지 매핑    │
                    │  · 공 소유권 권위 중재       │
                    │  · 메시지 중계 + 프레즌스    │
                    └───▲──────────▲──────────▲───┘
              WSS(TLS)  │          │          │  (모두 아웃바운드)
              ┌─────────┘          │          └─────────┐
        ┌─────┴──────┐      ┌──────┴─────┐      ┌───────┴────┐
        │  PC-A(집)  │      │ PC-B(사무실)│      │ PC-C(노트북)│
        │ ThrowMe     │      │ ThrowMe     │      │ ThrowMe     │
        └────────────┘      └────────────┘      └────────────┘
```

**클라이언트(ThrowMe) 신규 컴포넌트**
```
App
 └─ SlimeWindow (렌더 루프: 소유 중일 때만 물리 tick)   ← 기존
      ├─ SlimePhysicsEngine           ← 기존, 변경 없음(순수 유지)
      ├─ NetworkedWalkableArea         ← 기존 설계 재사용(연결된 엣지 인지)
      └─ BallHandoffCoordinator        ← 기존 설계 재사용(교차 감지·패킹/언패킹)
 └─ RelayClient          (신규: 서버와 WSS 상시 연결·재연결·하트비트)
 └─ AuthService          (신규: 로그인·토큰 보관·갱신)
 └─ RoomConfig           (신규: 서버에서 받은 노드/엣지 매핑 캐시)
```

> 기존 LAN 설계의 `NetworkService`(PC↔PC 직접 TCP) 자리를 **`RelayClient`(PC↔서버 WSS)**가 대체한다.
> `BallHandoffCoordinator`는 "누구에게 보낼지"를 **상대 IP 대신 상대 nodeId**로만 바꾸면 재사용된다.

---

## 4. 기존 코드/설계와의 접점 (재사용 vs 신규)

| 기존 요소 | 처리 |
|-----------|------|
| `SlimePhysicsEngine` | **변경 없음.** 상태 직렬화만 사용. |
| `IWalkableArea` / `MonitorLayoutService` | **변경 없음.** 로컬 모니터 판정 유지. |
| `BallHandoffCoordinator`(LAN 설계) | **재사용.** 전송 대상만 `nodeId`로, 전송 수단만 `RelayClient`로 교체. |
| 좌표 정규화(t, 엣지 법선/접선, flip) | **그대로 재사용.** 해상도/DPI 무관 핵심. |
| 핸드오프 메시지 필드(BALL_HANDOFF 등) | **그대로 재사용.** 릴레이 봉투(envelope)에 담아 전송. |
| `NetworkService`(직접 TCP) | **미사용/대체** → `RelayClient`(WSS). |
| `ClusterConfig`(로컬 cluster.json) | **RoomConfig로 승격** → 서버가 그룹 전체에 일관 배포. |

---

## 5. 연결 계층: WebSocket over TLS

- **전송**: 각 PC → 서버로 **WSS(WebSocket Secure, TLS)** 상시 1개 연결.
- **NAT 통과 원리**: 클라이언트가 여는 아웃바운드 연결이라 공유기/방화벽이 별도 설정 없이 허용.
- **재연결**: 끊기면 지수 백오프로 자동 재연결 → 재인증 → 서버가 프레즌스·소유권 복구.
- **하트비트**: 클라이언트↔서버 ping/pong(예: 15~30s). 죽은 연결 감지.
- **직렬화**: `System.Text.Json`(기존 스택과 동일). 개행/프레임 단위 JSON.
- **표준 라이브러리만**: .NET `System.Net.WebSockets.ClientWebSocket` → **NuGet 불필요**(앱의 "외부 패키지 최소" 원칙 유지).

---

## 6. 로그인/인증 모델

두 단계로 나눠 **MVP는 0원·최소**, 이후 정식 계정으로 승격한다.

### 6.1 MVP — 방 코드 + 시크릿 (계정 DB 없이 0원)
- 사용자가 **Room Code**(예: `THROWME-A3F9`)와 **Room Secret**을 만들고, 참여할 모든 PC에 같은 값을 입력.
- 서버는 (코드+시크릿)이 일치하는 연결들을 **같은 방**으로 묶는다.
- 노드 식별: `nodeId = 사용자가 정한 이름`(예: `집-데스크톱`) + 기기 지문(설치 GUID).
- 장점: **계정 백엔드 불필요** → KV/메모리에 방 상태만. 결제 0원.
- 시크릿은 **연결 인증 + 메시지 서명 키**로도 사용(공 주입 방지, 13절).

### 6.2 정식 — 계정 로그인 (확장)
- OAuth/이메일 계정(무료 티어: Firebase Auth / Supabase Auth / Clerk 등).
- 계정 하위에 **여러 방** 소유·초대·기기 관리.
- 연결 시 **단기 JWT** 발급 → WSS 핸드셰이크에 첨부, 서버가 검증.
- 승격 시에도 **아래 프로토콜·핸드오프 로직은 불변**(인증 앞단만 교체).

> 결정: **MVP는 6.1로 시작**(빠르고 무료). 사용자가 붙으면 6.2로 승격. 프로토콜은 동일해 마이그레이션 저비용.

---

## 7. 방(Room) & 엣지 매핑 모델

- **Room** = 공을 공유하는 노드 집합 + **EdgeLink 목록**(어느 PC의 어느 엣지 → 어느 PC의 어느 엣지).
- EdgeLink 개념·좌표 변환은 LAN 설계 5절과 **동일**. 단, **저장 위치가 서버**로 바뀐다.
- **서버가 방 설정을 권위 배포** → 모든 PC가 **동일한 매핑**을 갖는다(로컬 파일 불일치 문제 제거).
- 온라인 노드만 링크가 활성. 오프라인 노드로 향하는 엣지는 **일반 벽(반사)로 폴백**.

```jsonc
// 서버 보관 Room 상태 (개념)
{
  "roomId": "THROWME-A3F9",
  "nodes": [
    { "nodeId": "집-데스크톱",  "online": true,  "hasBall": true  },
    { "nodeId": "사무실-PC",    "online": true,  "hasBall": false },
    { "nodeId": "노트북",       "online": false, "hasBall": false }
  ],
  "links": [
    { "from": "집-데스크톱", "fromEdge": "Right", "to": "사무실-PC", "toEdge": "Left",  "flip": false },
    { "from": "사무실-PC",   "fromEdge": "Left",  "to": "집-데스크톱","toEdge": "Right", "flip": false }
  ],
  "owner": "집-데스크톱",   // 공 소유 권위 상태
  "seq": 128
}
```

---

## 8. 공 소유권(토큰) — 서버 권위 중재

LAN 설계는 A↔B의 ACK로 소유권을 넘겼지만, 인터넷에선 **서버를 단일 권위**로 두어 더 견고하게 만든다.

- 서버가 방마다 `owner`(현재 공 소유 노드)와 `seq`를 **권위 상태**로 보관.
- **오직 서버가 인정한 owner만** 핸드오프를 시작할 수 있다 → **복제·유실·스플릿브레인 방지**.
- 소유권 이전은 서버가 커밋(원자적)하고 양쪽에 통지한다.

---

## 9. 메시지 프로토콜 (릴레이 봉투 + 기존 핸드오프 재사용)

모든 메시지는 **봉투(envelope)**로 감싸 서버가 라우팅한다. `data`에는 LAN 설계의 핸드오프 필드를 그대로 넣는다.

```jsonc
// 공통 봉투
{
  "v": 1,
  "type": "HANDOFF | ACK | HELLO | PRESENCE | HEARTBEAT | ROOM_CONFIG | ERROR",
  "roomId": "THROWME-A3F9",
  "from": "집-데스크톱",
  "to":   "사무실-PC",        // 서버 라우팅 대상(브로드캐스트면 생략)
  "seq":  128,
  "sig":  "hmac...",          // 6.1 시크릿/6.2 토큰 기반 서명(위변조 방지)
  "data": { ... }             // 아래 타입별 페이로드
}

// HANDOFF.data — LAN 설계 6절 BALL_HANDOFF 필드 그대로 재사용
{
  "handoffId": "guid",
  "viaLink": "집-데스크톱.Right->사무실-PC.Left",
  "edgeParam": 0.37,          // 진입 엣지 t (0~1)
  "normalSpeed": 1820.5,      // 엣지 법선 성분(항상 양수=안쪽)
  "tangentSpeed": -240.0,     // 접선 성분(부호=방향)
  "angularVelocity": 320.0,   // deg/s (필요 시 부호 변환 후)
  "surfaceSpin": -150.0,      // px/s (끌어치기/밀어치기)
  "surfaceSpinAxisDeg": 12.0, // SpinShotDir 각도(엣지 기준)
  "spinAngle": 47.0           // 시각 회전 연속성
}

// ACK.data
{ "handoffId": "guid", "accepted": true }

// PRESENCE.data (서버→클라이언트: 방 구성/온라인 상태 갱신)
{ "nodes": [ ... ], "owner": "집-데스크톱" }

// ROOM_CONFIG.data (서버→클라이언트: 엣지 매핑 배포)
{ "links": [ ... ] }
```

- **절대 좌표 미전송**(해상도/DPI 무관) — LAN 설계와 동일.
- `seq`/`handoffId`로 중복·유실 방지, `sig`로 위변조 방지.

---

## 10. 핸드오프 시퀀스 (서버 경유)

```
PC-A(owner)                 Relay Server                 PC-B
   │  공이 연결된 엣지 넘음 감지                          │
   │  물리 정지 + 슬라임 숨김(잠금)                        │
   │──HANDOFF(to:B, t,속도,스핀)──▶│                       │
   │                                │ owner==A? 검증 ✔     │
   │                                │ owner="in-transit"   │
   │                                │────HANDOFF──────────▶│
   │                                │        진입 엣지에서 공 생성
   │                                │        물리 시작
   │                                │◀────────ACK──────────│
   │                                │ owner=B 커밋(원자적) │
   │◀──ACK/RELEASE(owner=B)─────────│──PRESENCE(owner=B)──▶│
   │  로컬 공 완전 제거(소유권 해제)                       │
```

- **타임아웃(예: 700ms~1.5s, 인터넷 지연 고려)**: B가 ACK 안 하면 서버가 `owner=A`로 롤백 → A에 **RELEASE 취소** 통지 → A가 공을 **되돌려 반사**(유실 방지).
- **대상 오프라인**: 서버가 즉시 거부 → A는 그 엣지를 **일반 벽으로 반사**.
- **진입 보정**: 네트워크 지연 흡수를 위해 B는 진입 엣지 **살짝 안쪽**(슬라임 크기×0.5)에 생성 — LAN 설계 5.4 그대로.

---

## 11. 좌표/속도 정규화 (기존 재사용, 변경 없음)

- 엣지 로컬 파라미터 `t∈[0,1]`, 엣지 **법선 n·접선 τ** 분해, `flip` 시 `t→1-t`·접선 부호 반전.
- 각속도(AngularVelocity)·`SpinShotDir` 부호/회전 변환도 LAN 설계 5.3과 동일.
- → **서로 다른 해상도/DPI의 집·사무실 PC 사이에서도 진입 비율·크기·스핀이 일관**.

---

## 12. 폴백 / 에러 처리

| 상황 | 처리 |
|------|------|
| 서버 연결 끊김 | 그 PC는 **로컬 토이로 계속 동작**(반사). 재연결 시 자동 복구. |
| 대상 노드 오프라인 | 해당 엣지 **일반 벽으로 반사**. 공 유실 없음. |
| ACK 타임아웃 | 서버가 owner 롤백 → A가 **공 되돌려 반사**. |
| 소유권 경합(중복 시작) | 서버가 `owner`/`seq`로 **단일 승자만 인정**, 나머지 거부. |
| 중복 수신(seq 재도착) | 무시(idempotent). |
| 서버 재시작 | 클라이언트 재연결·재인증, 방 상태 재수립. 진행 중 핸드오프는 타임아웃→반사로 안전 종료. |
| 버전 불일치(HELLO) | 경고 로그 + 정책적 거부/허용. |

> 모든 네트워크 예외는 기존 `Logger`로 기록하고 **앱은 절대 죽지 않는다**(로컬 슬라임은 항상 동작).

---

## 13. 보안 / 개인정보

- **전송 암호화**: **WSS(TLS) 필수**. 평문 금지.
- **인증**: MVP=Room Secret 기반 HMAC 서명, 정식=단기 JWT. 서버가 봉투 `sig` 검증.
- **공 주입 방지**: 서버가 **현재 owner가 보낸 HANDOFF만** 수용. 방 미소속 노드 메시지 폐기.
- **레이트리밋 / 메시지 크기 상한**: 서버에서 남용 차단.
- **전송 데이터**: 공의 **물리 상태(엣지 비율·속도·스핀)뿐** — 개인정보 없음.
- **로그인 정보**: 계정 승격 시 이메일 등 최소한만, 서버 측 안전 저장(무료 티어 인증 서비스 위임 시 그쪽 정책 준수).
- **비밀값 미하드코딩**: Room Secret/토큰은 `%LOCALAPPDATA%\ThrowMe` 등 로컬 보관, 리포지토리·배포 exe에 넣지 않음.

---

## 14. 서버 구현 선택지 & 호스팅 (무료 시작)

핸드오프 트래픽은 **작고 드물다** → 작은 인스턴스 하나로 충분. **결제 0원**으로 시작 가능.

| 스택 | 성격 | 무료 | 운영 공수 | 비고 |
|------|------|:---:|:---:|------|
| **Cloudflare Workers + Durable Objects** ⭐ | 서버리스, 방=DO(상태 보관) | ✅ 넉넉 | **낮음**(VM 유지 불필요) | WSS·TLS 자동. 방별 상태·소유권에 이상적. JS/TS. |
| Node.js `ws` (또는 Deno/Go) on **무료 VM** | 자체 상시 서버 | ✅ (Oracle always-free 등) | 중(패치·가동관리) | 완전 제어. |
| ASP.NET Core Minimal API + WebSockets | C# 일관 | 일부 | 중 | 앱과 같은 언어. 소형 VPS. |

- **TLS**: Let's Encrypt(무료) 또는 플랫폼 자동(Cloudflare/fly.io).
- **도메인**: 무료 서브도메인(`*.workers.dev`, `*.fly.dev`) 또는 연 1만원대 커스텀.
- **추천(MVP)**: **Cloudflare Workers + Durable Objects** — 상시 VM을 안 굴려도 되어 **운영 공수·비용이 가장 낮음**, 방 상태·WSS가 자연스럽게 맞음. (자체 제어를 원하면 무료 VM + Go/Node.)

---

## 15. 비용 (규모별)

| 규모 | 서버 | 도메인 | 인증 | 월 비용(대략) |
|------|------|--------|------|------|
| 나 + 내 PC 몇 대 | 무료 티어 | 무료 서브도메인 | Room Secret | **0원** |
| 지인 수십 명 | 무료 티어~$5 VPS | 0~연 1만원대 | Room Secret/무료 Auth | **0~수천원** |
| 불특정 배포 | 스펙업 VPS/서버리스 유료 구간 | 커스텀 | 유료 Auth 구간 | 수천~수만원 + **운영 책임** |

> **진짜 비용은 결제가 아니라 공수**: ① 서버·인증·클라이언트 연동(일회성) ② 서버 가동/보안/문의 대응(지속). 돈이 0원이어도 **운영 부담은 남는다**.

---

## 16. 신규 / 수정 파일 목록

**클라이언트(ThrowMe) — 신규**
- `Network/RelayClient.cs` — 서버 WSS 상시 연결·재연결·하트비트·봉투 송수신
- `Network/AuthService.cs` — 로그인(Room Code+Secret / 이후 토큰)·비밀값 로컬 보관
- `Network/RoomConfig.cs` — 서버가 배포한 노드·엣지 매핑 캐시
- `Network/RelayMessages.cs` — 봉투·페이로드 DTO(직렬화 대상)
- `Network/BallHandoffCoordinator.cs` — **LAN 설계 재사용**(전송 대상 nodeId·수단 RelayClient로)
- `Physics/NetworkedWalkableArea.cs` — **LAN 설계 재사용**(연결된 엣지 인지)
- (설정 UI: `Views/RelaySettings*` — 로그인·방 코드·엣지 매핑 편집. **디자인 트랙**)

**클라이언트 — 수정(최소)**
- `SlimeWindow.xaml.cs` — 렌더 루프에 `Coordinator.CheckHandoff()` 연결, 비소유 시 유휴
- `App.xaml.cs` — `AuthService`/`RelayClient`/`RoomConfig` 생성·해제

**변경 없음**: `SlimePhysicsEngine`(순수 유지), `MonitorLayoutService`, `AudioService`, `ParticleSystem`

**서버 — 신규 (별도 리포지토리 권장)**
- 릴레이 서버(선택 스택): 인증·Room 상태·소유권 중재·메시지 라우팅·프레즌스·하트비트

---

## 17. 구현 단계 (Phase 7)

1. **7-A 서버 뼈대**: 릴레이 서버 배포(무료 티어) + WSS 에코 + Room Code/Secret 인증. 클라이언트 `RelayClient` 연결·하트비트.
2. **7-B 프레즌스**: 방 참여·온라인 목록·`ROOM_CONFIG` 배포. 두 PC가 서로 "보이는지" 확인.
3. **7-C 최소 핸드오프**: A.Right→B.Left 한 방향, t+속도만(스핀 제외), **서버 권위 소유권** 이전 검증.
4. **7-D 양방향 + 스핀/각속도 변환** + 진입 보정.
5. **7-E 임의 엣지 매핑(90° 회전 포함) + flip** — 집↔사무실 자유 배치.
6. **7-F 폴백/타임아웃/재연결/오프라인 반사** 안정화.
7. **7-G 보안 강화**(HMAC 서명·레이트리밋) + **로그인 승격**(정식 계정, 선택).
8. **7-H 설정 UI**(로그인·방·엣지 매핑 편집) — 디자인 트랙.

각 단계마다 **빌드·실행 유지**, 서버 없거나 오프라인이어도 로컬 슬라임은 정상 동작.

---

## 18. 테스트 체크리스트

- [ ] 서로 다른 네트워크(집 회선 ↔ 다른 집/사무실 회선)의 두 PC가 **로그인만으로 연결**
- [ ] NAT/방화벽 별도 설정 없이 연결(포트포워딩 불필요)
- [ ] A 오른쪽으로 던지면 B 왼쪽에서 **같은 높이 비율·속도 이어짐**
- [ ] 되돌아오기(양방향), 3대 체인(A→B→C)
- [ ] 임의 매핑 A.Right→B.Top(90°) 자연스럽게 꺾여 진입, flip(1-t) 반전
- [ ] 스핀(사이드/끌어치기) 상태가 넘어가서도 이어짐
- [ ] **소유권: 어느 순간에도 공은 정확히 한 대**(복제·유실 없음)
- [ ] 대상 PC 오프라인 → 그 엣지 **반사**(공 안 잃음)
- [ ] ACK 타임아웃 → 공 되돌림
- [ ] 서버 끊김/재시작 → 로컬은 계속 동작, 재연결 자동 복구
- [ ] 비소유 PC: 슬라임 숨김 + **CPU ≈ 0**
- [ ] 해상도/DPI 다른 집·사무실 PC 간 진입 비율·크기 일관
- [ ] WSS(TLS)로만 통신, 방 미소속/위변조 메시지 거부
- [ ] 인터넷 지연(수십~수백 ms)에서도 진입 보정으로 "쑥 들어오는" 연출 자연스러움

---

## 18.5 구현 현황 (2026-07-25)

- **서버(7-A/7-B/7-C 핵심) 완료** — `server/slimey-relay/` (Cloudflare Workers + Durable Objects).
  - `index.ts`(라우팅) · `room.ts`(Room DO: 인증·프레즌스·소유권 중재·핸드오프 라우팅·타임아웃 알람) · `protocol.ts`(메시지 계약).
  - 검증: `tsc` 0오류, `wrangler deploy --dry-run` OK, **로컬 dev E2E 9/9 PASS**
    (방 생성·시크릿 인증·BAD_SECRET 거부·NOT_OWNER·핸드오프 라우팅·소유권 이전·TARGET_OFFLINE 폴백·ACK 타임아웃 롤백·소유자 이탈 이양).
- **클라이언트 연결 계층 완료** — `src/ThrowMe/Network/`.
  - `RelayMessages.cs`(DTO, 서버 protocol.ts 와 1:1) · `AuthService.cs`(방 코드/시크릿/노드id, `relay.json` 영속화, WSS URL·HELLO 빌드) · `RelayClient.cs`(WSS 상시 연결·지수 백오프 재연결·하트비트·송수신 이벤트).
  - 검증: `dotnet build` 0경고/0오류. **아직 App/SlimeWindow 에 미배선**(다음 단계).
- **물리 통합(7-C/7-D/7-E 핵심) 완료** — `src/ThrowMe/Network/`.
  - `HandoffMath.cs`(순수: 엣지 법선/접선, t 정규화·역변환, 교차 감지, pack/unpack) · `NetworkedWalkableArea.cs`(연결된 엣지로는 나가도 유효 → 반사 대신 통과) · `BallHandoffCoordinator.cs`(교차 감지→전송, ACK 결과 처리·롤백 반사, 수신 주입).
  - `SlimeWindow` 배선: 생성자에서 릴레이 설정 시 `_physics.Area` 를 `NetworkedWalkableArea` 로 교체 + 코디네이터·RelayClient 생성, 렌더 루프에 `CheckAndSendHandoff()`, 소유권에 따른 표시/숨김/유휴(`OnRelayMessage`/`ApplyOwnership`). 릴레이 미설정 시 기존 로컬 동작 그대로.
  - 검증: `dotnet build` 0/0 · **HandoffMath 단위 17/17 PASS**(직선·90°·flip·이종해상도) · **클라이언트↔서버↔클라이언트 통합 11/11 PASS**(실 RelayClient 2노드가 실서버 거쳐 공 전달, 진입 속도/위치가 JSON 왕복 후 정확히 일치).
- **설정 UI(7-H) 완료** — 설정창에 **"멀티 PC" 탭** 추가.
  - `Resources/Theme.xaml`: 다크 입력 스타일 `DarkTextBox`/`DarkPassword`/`DarkCombo`(+`DarkComboItem`) 신규.
  - `Views/SettingsWindow`: 사용 토글·서버주소·방 코드·시크릿(PasswordBox)·이 PC 이름 + **엣지 매핑 4행**(이 PC의 왼/오/위/아래 → 상대 노드·엣지·거울) + 연결 상태 표시 + "저장 & 적용".
  - `SlimeWindow`: 런타임 재설정 API(`ApplyRelaySettings`/`PushRoomConfig`/`RelayAuth`/`RelayState`/`RelayStateChanged`) + `SetupNetworking`/`TeardownNetworking`(런타임 on/off). WELCOME 시 서버 링크 없으면 로컬 매핑으로 방 시딩.
  - `AuthService`: `Links` 영속화(relay.json) 추가.
  - 검증: `dotnet build` 0/0 · **WPF 렌더 하네스 전체 PASS**(Theme 파싱·다크 콤보 드롭다운·멀티 PC 탭 4행 표시).
- **실배포 완료** — `wss://slimey-relay.throwme.workers.dev` (Cloudflare Workers + DO, 무료 티어).
  `/healthz` 200 · WSS 핸드셰이크·인증·방 생성 확인.
- **링크 병합 버그 수정** — `Network/LinkMerge.cs` 신규.
  - 증상: A→B 첫 전달만 되고 이후 막힘. 원인: 나중에 접속한 노드가 **서버 링크를 그대로 채택하며 자기 매핑을 버려** 나갈 엣지가 0이 됨(→ 벽 반사).
  - 규칙: **자기 노드에서 나가는 링크는 그 PC 가 권위**, 다른 노드 링크는 서버 것 보존. 병합 결과가 서버와 다르면 `ROOM_CONFIG` 재배포.
  - 검증(배포 서버 경유): A/B 각자 자기 링크만 갖고 접속 → 양쪽 출구 확보 → **A→B→A 왕복 + 반복 PASS**.
- **한 PC 다중 인스턴스 테스트 지원** — `--profile=<이름>` 으로 설정 파일 분리(`relay.<p>.json`/`settings.<p>.json`),
  `--server/--room/--secret/--node/--link` 실행 인자 오버라이드(`AuthService`), 실행 스크립트 `tools/test-two-nodes.ps1`.
  - **주의(설정 지침)**: 좌·우 **양쪽 모두** 상대에게 매핑해야 어느 방향으로든 넘어간다.
    한쪽만 연결하면 공을 받은 노드는 반대 방향만 열려 "번갈아" 동작한다(연결 없는 엣지는 설계대로 반사).
- **다음 단계(미구현)**:
  1. 실제 2대 PC 실측(인터넷 지연·진입 보정 체감).
  2. 보안 강화(7-G): 메시지 HMAC 서명(`sig`).
  3. (선택) 프레즌스 기반 상대 PC 드롭다운(현재는 이름 직접 입력).

---

## 19. 결론

- **가능하며, 기존 저침습 설계와 잘 맞는다.** 물리 엔진·좌표 정규화·토큰·핸드오프 메시지는 **그대로 재사용**하고, **연결 계층만 릴레이 서버(WSS)로 교체**한다.
- **NAT/방화벽 문제는 아웃바운드 WSS로 원천 회피**, 소유권은 **서버 권위 중재**로 더 견고.
- **결제는 규모에 비례**하며 **0원 프로토타입**부터 시작 가능. **가장 큰 비용은 개발·운영 공수**.
- 다음 실행 시 **Phase 7-A(서버 뼈대) + 7-B(프레즌스) + 7-C(최소 핸드오프)**부터 구현 권장.
